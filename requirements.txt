# ============================================================
# Chicago City Workforce Analytics
# Python Dependencies
# ============================================================
# Install with:  pip install -r requirements.txt
# Python version: 3.9+  (tested on 3.11)
# ============================================================

# ── Core Data ──────────────────────────────────────────────
pandas>=2.0.0
numpy>=1.24.0

# ── Visualization ──────────────────────────────────────────
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0

# ── Statistical Analysis ───────────────────────────────────
scipy>=1.10.0
statsmodels>=0.14.0

# ── Excel Export ───────────────────────────────────────────
openpyxl>=3.1.0
xlsxwriter>=3.1.0

# ── Notebook ───────────────────────────────────────────────
jupyter>=1.0.0
ipykernel>=6.0.0
nbformat>=5.0.0

# ── Data Serialization ─────────────────────────────────────
pyarrow>=12.0.0       # Parquet export (optional but recommended)

# ── Database ───────────────────────────────────────────────
# sqlite3 is built into Python standard library — no install needed

# ── Utilities ──────────────────────────────────────────────
python-dateutil>=2.8.0
tqdm>=4.65.0          # Progress bars for long-running operations

# ── AI/LLM Integration (Optional) ─────────────────────────
# Uncomment to enable live AI API calls:
# openai>=1.0.0       # GPT-4 integration
# anthropic>=0.20.0   # Claude integration

# ── Code Quality (Dev) ─────────────────────────────────────
# black>=23.0.0       # Code formatter
# flake8>=6.0.0       # Linter
# pytest>=7.0.0       # Unit testing
